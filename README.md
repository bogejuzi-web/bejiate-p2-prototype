#  P2 独立原型

此副本只保留以下入口：`N01`、`NM01`、`A01`、`NM11`、`N10`、`A11`、`A12`。`N11` 为活动申请通知的必要隐藏页面，不在左侧导航显示。

## 启动

需要 Node.js 18 或更高版本。首次运行执行：

```powershell
npm install
npm run dev
```

然后在浏览器打开终端显示的本地地址（通常为 `http://localhost:4173/`）。

演示账号：护士端 `nurse01 / 123456`；管理后台 `admin / 123456`。
